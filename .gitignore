# Dependencies
node_modules/

# Build / test output
coverage/
*.tsbuildinfo

# Editor / OS
.DS_Store
*.log

# Local env
.env
.env.local

# Professor course profiles — kept local (interim for the web-app feature).
# The example template is tracked so the shape is documented.
/profiles/*
!/profiles/example.profile.yaml
